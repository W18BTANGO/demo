import React from 'react';
import Typography from '@mui/material/Typography';

function Retrieve() {
    return (
        <Typography variant="h4">Retrieve Page</Typography>
    );
}

export default Retrieve;
