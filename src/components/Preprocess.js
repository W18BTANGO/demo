import React from 'react';
import Typography from '@mui/material/Typography';

function Preprocess() {
    return (
        <Typography variant="h4">Preprocess Page</Typography>
    );
}

export default Preprocess;
