import React from 'react';
import Typography from '@mui/material/Typography';

function Analyse() {
    return (
        <Typography variant="h4">Analyse Page</Typography>
    );
}

export default Analyse;
