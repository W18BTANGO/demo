import React from 'react';
import Typography from '@mui/material/Typography';

function Collect() {
    return (
        <Typography variant="h4">Collect Page</Typography>
    );
}

export default Collect;
